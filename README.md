# brackeeters375
Class project for CS 375 (Web Development) @ Drexel University.

# Runbook
```
# From the project root/src:
On first up, run npm i.
Then you should be able to run the application via npm run setup_local && npm run start.
```
